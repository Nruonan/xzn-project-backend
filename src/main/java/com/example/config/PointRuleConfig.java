package com.example.config;

import org.springframework.context.annotation.Configuration;

/**
 * 积分规则配置类
 * 积分规则现在从数据库的pointRule表中获取
 * @author Nruonan
 */
@Configuration
public class PointRuleConfig {
    // 积分规则已改为从数据库获取，此类保留作为占位符
}